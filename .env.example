# This root file documents where runtime configuration belongs.
# Copy the environment examples inside customer-app, admin-app, and server.
CUSTOMER_ENV=customer-app/.env
ADMIN_ENV=admin-app/.env
SERVER_ENV=server/.env
