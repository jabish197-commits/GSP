import{createContext,useContext,useEffect,useMemo,useState}from"react";import{login,getMe,logout}from"../services/authApi.js";const Context=createContext(null);export function AdminAuthProvider({children}){const[admin,setAdmin]=useState(null),[loading,setLoading]=useState(true);useEffect(()=>{getMe().then(d=>setAdmin(d.admin)).catch(()=>{}).finally(()=>setLoading(false))},[]);const value=useMemo(()=>({admin,loading,signIn:async credentials=>{const d=await login(credentials);setAdmin(d.admin)},signOut:async()=>{await logout();setAdmin(null)}}),[admin,loading]);return <Context.Provider value={value}>{children}</Context.Provider>}export const useAdminAuth=()=>useContext(Context);
