import{createContext,useContext,useMemo,useState}from"react";const Context=createContext(null);export function AdminChatProvider({children}){const[chats,setChats]=useState([]),[selected,setSelected]=useState(null);const value=useMemo(()=>({chats,setChats,selected,setSelected}),[chats,selected]);return <Context.Provider value={value}>{children}</Context.Provider>}export const useAdminChat=()=>useContext(Context);
