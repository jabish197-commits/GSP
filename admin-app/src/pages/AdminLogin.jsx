import{useState}from"react";export default function AdminLogin({onLogin}){const[form,setForm]=useState({email:"",password:""});return <main className="login"><section><img src="/logo.png" alt=""/><h1>Admin login</h1><form onSubmit={e=>{e.preventDefault();onLogin(form)}}><input type="email" placeholder="Email" required onChange={e=>setForm({...form,email:e.target.value})}/><input type="password" placeholder="Password" required onChange={e=>setForm({...form,password:e.target.value})}/><button>Sign in</button></form></section></main>}
