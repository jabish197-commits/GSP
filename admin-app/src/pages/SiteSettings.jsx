import{useState}from"react";export default function SiteSettings({settings={},onSave}){const[form,setForm]=useState(settings);return <form className="panel settings" onSubmit={e=>{e.preventDefault();onSave(form)}}><h2>Site settings</h2><label>Headline<input value={form.heroTitle||""} onChange={e=>setForm({...form,heroTitle:e.target.value})}/></label><label>Phone<input value={form.phone||""} onChange={e=>setForm({...form,phone:e.target.value})}/></label><label>Email<input value={form.email||""} onChange={e=>setForm({...form,email:e.target.value})}/></label><button>Save</button></form>}
