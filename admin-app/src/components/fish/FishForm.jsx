import{useState}from"react";const empty={name:"",strain:"",description:"",price:"",quantity:1,sex:"pair",status:"available",featured:false,media:[]};export default function FishForm({fish,onSave,onCancel}){const[form,setForm]=useState({...empty,...fish});const field=key=>e=>setForm({...form,[key]:e.target.type==="checkbox"?e.target.checked:e.target.value});return <form className="fish-form" onSubmit={e=>{e.preventDefault();onSave({...form,price:Number(form.price),quantity:Number(form.quantity)})}}><label>Name<input required value={form.name} onChange={field("name")}/></label><label>Strain<input required value={form.strain} onChange={field("strain")}/></label><label>Price<input type="number" min="0" required value={form.price} onChange={field("price")}/></label><label>Quantity<input type="number" min="0" value={form.quantity} onChange={field("quantity")}/></label><label>Description<textarea required value={form.description} onChange={field("description")}/></label><button>Save</button><button type="button" onClick={onCancel}>Cancel</button></form>}
