import { useState } from "react";
export default function EnquiryForm({ onSubmit, disabled }) { const [form,setForm]=useState({name:"",phone:"",email:"",address:"",notes:""}); const field=(key)=>(e)=>setForm({...form,[key]:e.target.value}); return <form onSubmit={e=>{e.preventDefault();onSubmit(form)}}><label>Name<input required value={form.name} onChange={field("name")}/></label><label>Phone / WhatsApp<input required value={form.phone} onChange={field("phone")}/></label><label>Email<input type="email" value={form.email} onChange={field("email")}/></label><label>Location<textarea value={form.address} onChange={field("address")}/></label><label>Message<textarea value={form.notes} onChange={field("notes")}/></label><button disabled={disabled}>Send enquiry</button></form>; }
