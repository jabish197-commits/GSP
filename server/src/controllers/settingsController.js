import SiteSettings from"../models/SiteSettings.js";export async function getSettings(_request,response){response.json({settings:await SiteSettings.findOneAndUpdate({key:"main"},{$setOnInsert:{key:"main"}},{upsert:true,returnDocument:"after"})})}export async function updateSettings(request,response){response.json({settings:await SiteSettings.findOneAndUpdate({key:"main"},request.body,{upsert:true,returnDocument:"after",runValidators:true})})}
