const required=["MONGODB_URI","JWT_ACCESS_SECRET","COOKIE_SECRET"];export function validateEnvironment(){const missing=required.filter(key=>!process.env[key]);if(missing.length)throw new Error(`Missing environment variables: ${missing.join(", ")}`)}export const environment={get nodeEnv(){return process.env.NODE_ENV||"development"},get isProduction(){return process.env.NODE_ENV==="production"},get port(){return Number(process.env.PORT||5000)}};
