import{z}from"zod";export const fishBody=z.object({name:z.string().min(2),strain:z.string().min(2),description:z.string().min(10).max(3000),price:z.number().nonnegative(),quantity:z.number().int().nonnegative(),sex:z.enum(["male","female","pair","juvenile","mixed"]),age:z.string().optional(),status:z.enum(["available","reserved","sold"]),featured:z.boolean().optional(),media:z.array(z.object({url:z.string().url(),publicId:z.string().optional(),type:z.enum(["image","video"]),alt:z.string().optional()})).optional()});export const createFishSchema=z.object({body:fishBody,params:z.object({}),query:z.object({})});
