import mongoose from"mongoose";const schema=new mongoose.Schema({sessionId:{type:String,required:true,index:true},customerName:{type:String,default:"Guest"},customerPhone:String,status:{type:String,enum:["pending","accepted","active","closed"],default:"pending"},acceptedAt:Date,closedAt:Date},{timestamps:true});export default mongoose.models.ChatRequest||mongoose.model("ChatRequest",schema);
