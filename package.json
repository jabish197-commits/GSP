{
  "name": "sj_guppy_paradise",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "customer-app",
    "admin-app",
    "server",
    "shared"
  ],
  "scripts": {
    "dev:customer": "npm run dev --workspace customer-app",
    "dev:admin": "npm run dev --workspace admin-app",
    "dev:server": "npm run dev --workspace server",
    "build": "npm run build --workspace customer-app && npm run build --workspace admin-app",
    "check": "npm run build && npm run check --workspace server && npm test --workspace server",
    "test": "npm test --workspace server"
  }
}
